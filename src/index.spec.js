describe('GIVEN as user', () => {
  describe('WHEN I want to test my async redux actions and state', () => {
    test('THEN I can use actionsToStateSnapshot', () => {
      expect(true).toEqual(true)
    })
  })
})
